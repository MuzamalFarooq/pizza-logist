import LoginForm from "./LoginForm";

export const metadata = {
  title: "Admin Login | Pizza Logist Panel",
  description: "Secure administrative login portal for Pizza Logist restaurant panel. Authorized personnel only.",
};

export default function LoginPage() {
  return <LoginForm />;
}
